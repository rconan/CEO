include ../common.mk

nwsrc = filename.nw

all: src lib tex

src: $(nwsrc) $(header)

lib: $(obj)
#	$(AR) rcs $(libsrc) $(obj)

cysrc: $(nwsrc) $(pxdsrc) $(pyxsrc)

cylib:
	make -C $(CEOPYPATH) $(sobj)

tex: $(texsrc)

cleanpython:
	rm -f *.pxd *.pyx *.so

clean:
	rm -f $(obj) $(header) *.*~ *.tex *.pxd *.pyx *.so

cleanbins:
	rm -f bins/*

